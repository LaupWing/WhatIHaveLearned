import firebase from 'firebase'
import db from '@/firebase/init'

const state = {
    
}

const getters = {
}

const actions = {
}

const mutations = {
    
}

export default{
    state,
    actions,
    getters,
    mutations
}